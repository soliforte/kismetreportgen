name=reportgen
description=Create a report of SSIDs, BSSIDs and Client devices.
author=Soli Forte <soliforte@protonmail.com>
version=2026-09-R1

# Web UI module loaded by the main Kismet UI (adds the sidebar entry).
# Kismet requires this path to be relative (no leading '/') so that the UI
# works behind a reverse proxy prefix.
js=reportgen,plugin/reportgen/js/reportgen.js
